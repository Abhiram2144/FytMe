# Vite environment variables for backend integration
# Rename to .env and set values accordingly.
# Vite exposes variables prefixed with VITE_

# Base URL for API endpoints
# Example: https://your-backend.onrender.com/api
VITE_API_BASE_URL=https://your-backend.onrender.com/api

# Base URL for image serving
# Example: https://your-backend.onrender.com/api/images
# Optional: defaults to `${VITE_API_BASE_URL}/images` if not set
VITE_IMAGE_BASE_URL=https://your-backend.onrender.com/api/images
